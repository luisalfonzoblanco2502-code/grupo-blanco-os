// Spinner inline para botones en estado de carga — mismo lenguaje visual en
// toda la app en vez de que cada formulario invente su propio "..." o texto.
export function Spinner() {
  return <span className="spinner" aria-hidden="true" />;
}
